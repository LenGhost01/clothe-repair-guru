# 通用配置
#当前激活的环境
spring:
  profiles:
    active: dev

#mybatis配置
mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.chenhaozhe.clothe_guru_code.model.entity
